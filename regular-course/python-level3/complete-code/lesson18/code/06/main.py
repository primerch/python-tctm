'''

我需要一段Python代码，它使用PyQt5库来创建一个名为"超级背单词"的主窗口应用。
在应用中首先从learned模块导入LearnedWords类
从recite模块导入ReciteWords类
从search模块导入SearchWords类
这个应用默认会创建ReciteWords对象，然后设置为窗口的中心部件。
这个应用需要有一个菜单栏，菜单栏中有一个选项：'切换窗口'。
点击切换窗口时会出现三个选项'背单词'，'查单词'和'已会单词'。
每个选项被触发时，都会显示一个新的中心部件。
例如，当'背单词'被触发时，创建ReciteWords对象，并设为新的中心部件；
'查单词'被触发时，创建SearchWords对象，并设为新的中心部件；
'已会单词'被触发时，创建LearnedWords对象，并设为新的中心部件；
应用的字体应为Arial，大小为16，窗口的固定大小为1200x675。
返回代码并为每行代码添加注释

'''
