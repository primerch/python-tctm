with open()语句中常用的文件操作模式有以下几种：
'r'：只读模式，用于读取文件内容，也是默认的操作模式，也就是说当文件操作模式不写，默认使用的就是这个模式。
'w'：写入模式，用于创建新文件或覆盖已存在的文件。
'a'：追加模式，用于在文件末尾追加内容。
'x'：独占创建模式，用于创建新文件，如果文件已存在则抛出错误。
'b'：二进制模式，用于以二进制方式读写文件。
除了上述常见的模式外，还可以结合使用模式来满足特定需求。例：，'rb' 表示以二进制格式打开一个文件用于只读，'wb' 表示以二进制格式打开一个文件只用于写入。
